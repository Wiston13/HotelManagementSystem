@model ErrorViewModel
@{
    ViewData["Title"] = "系統錯誤";
}

<h1 class="text-danger">系統發生錯誤</h1>
<p class="text-danger">請稍後再試。</p>

@if (Model.ShowRequestId)
{
    <p>
        <strong>請求識別碼：</strong> <code>@Model.RequestId</code>
    </p>
}
