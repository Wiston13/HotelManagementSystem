@* 飯店後台：基本操作紀錄查詢頁 (23種操作類型完全體) *@
@{
    Layout = "~/Views/Shared/_ManagerLayout.cshtml";
    ViewData["Title"] = "操作紀錄";
}

<div class="container py-4">
    <!-- 頁面標題區塊 -->
    <h1 class="h3 fw-bold mb-2">基本操作紀錄查詢</h1>
    <p class="text-secondary mb-4">查詢系統內各帳號的操作歷程紀錄</p>

    <!-- 查詢條件區塊 (Card) -->
    <div class="card mb-4 shadow-sm">
        <div class="card-body">
            <form id="searchForm" onsubmit="return false;">
                <!-- 第一排：日期區間、分館、操作類型 -->
                <div class="row g-3 mb-3">
                    <div class="col-12 col-md-4">
                        <label class="form-label">
                            日期區間 <span class="text-danger">*</span>
                        </label>
                        <div class="input-group">
                            <input type="date" class="form-control" id="dateStart" value="2026-08-10">
                            <span class="input-group-text bg-light text-secondary">至</span>
                            <input type="date" class="form-control" id="dateEnd" value="2026-08-11">
                        </div>
                    </div>

                    <div class="col-12 col-md-4">
                        <label class="form-label">
                            分館 <span class="text-danger">*</span>
                        </label>
                        <select class="form-select" id="branchSelect">
                            <option value="全部分館">全部分館</option>
                            <option value="台北商旅">台北商旅</option>
                            <option value="台中商旅">台中商旅</option>
                            <option value="高雄商旅">高雄商旅</option>
                        </select>
                    </div>

                    <div class="col-12 col-md-4">
                        <label class="form-label">
                            操作類型 <span class="text-danger">*</span>
                        </label>
                        <!-- 【核心修正】這裡已百分之百補齊 23 種正式中文操作類型 -->
                        <select class="form-select" id="typeSelect">
                            <option value="全部類型" selected>全部類型</option>

                            <optgroup label="🏢 分館與房型管理">
                                <option value="建立分館">建立分館</option>
                                <option value="修改分館">修改分館</option>
                                <option value="開放新訂房">開放新訂房</option>
                                <option value="停止新訂房">停止新訂房</option>
                                <option value="建立房型">建立房型</option>
                                <option value="修改房型">修改房型</option>
                                <option value="停用房型">停用房型</option>
                                <option value="啟用房型">啟用房型</option>
                            </optgroup>

                            <optgroup label="🔑 房間與保留控制">
                                <option value="建立房間">建立房間</option>
                                <option value="修改房間">修改房間</option>
                                <option value="停用房間">停用房間</option>
                                <option value="啟用房間">啟用房間</option>
                                <option value="設為保留">設為保留</option>
                                <option value="解除保留">解除保留</option>
                                <option value="更新清潔狀態">更新清潔狀態</option>
                            </optgroup>

                            <optgroup label="👤 員工帳號管理">
                                <option value="建立帳號">建立帳號</option>
                                <option value="修改帳號">修改帳號</option>
                                <option value="停用帳號">停用帳號</option>
                                <option value="啟用帳號">啟用帳號</option>
                                <option value="重設密碼">重設密碼</option>
                            </optgroup>

                            <optgroup label="🛎️ 訂單與日常櫃檯">
                                <option value="取消訂單">取消訂單</option>
                                <option value="Check-in">Check-in</option>
                                <option value="Check-out">Check-out</option>
                            </optgroup>
                        </select>
                    </div>
                </div>

                <!-- 第二排：操作者、操作對象、查詢按鈕 -->
                <div class="row g-3 align-items-end">
                    <div class="col-12 col-md-4">
                        <label class="form-label">操作者 <span class="text-secondary">＊選填</span></label>
                        <input type="text" class="form-control" id="operatorInput" placeholder="姓名 / 帳號">
                    </div>

                    <div class="col-12 col-md-4">
                        <label class="form-label">操作對象 <span class="text-secondary">＊選填</span></label>
                        <input type="text" class="form-control" id="targetInput" placeholder="編號 / 房號">
                    </div>

                    <div class="col-12 col-md-4 d-grid">
                        <button type="button" class="btn btn-primary" id="btnSearch">
                            查詢
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- 查詢結果列表區塊 -->
    <div class="card mb-4 shadow-sm d-none" id="resultCard">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th style="width: 12%;">時間</th>
                        <th style="width: 12%;">分館</th>
                        <th style="width: 12%;">操作者</th>
                        <th style="width: 15%;">操作類型</th>
                        <th style="width: 12%;">操作對象</th>
                        <th style="width: 12%;">識別編號</th>
                        <th style="width: 25%;">說明</th>
                    </tr>
                </thead>
                <tbody id="resultTableBody"></tbody>
            </table>
        </div>
    </div>

    <!-- 查無資料的友善提示區塊 -->
    <div class="card mb-4 shadow-sm text-center py-5 d-none" id="noDataCard">
        <div class="card-body">
            <div class="text-secondary mb-3" style="font-size: 2.5rem;">🔍</div>
            <h5 class="fw-bold mb-2">很抱歉，在此篩選條件下查無任何資料</h5>
            <p class="text-muted small mb-0">請嘗試調整您的查詢日期或篩選條件後重新查詢。</p>
        </div>
    </div>
</div>


@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            const dateStart = document.getElementById("dateStart");
            const dateEnd = document.getElementById("dateEnd");
            const btnSearch = document.getElementById("btnSearch");
            const resultCard = document.getElementById("resultCard");
            const noDataCard = document.getElementById("noDataCard");
            const resultTableBody = document.getElementById("resultTableBody");

            const branchSelect = document.getElementById("branchSelect");
            const typeSelect = document.getElementById("typeSelect");
            const operatorInput = document.getElementById("operatorInput");
            const targetInput = document.getElementById("targetInput");

            // 1. 原始模擬資料集 (已補齊欄位)
            const mockData = [
                {
                    date: "2026-08-10",
                    displayTime: "08/10 16:05",
                    branch: "台北商旅",
                    operator: "林怡君",
                    type: "解除保留",
                    target: "房間",
                    targetIdentifier: "304",
                    description: "房間 304 已解除保留並恢復開放販售"
                },
                { date: "2026-08-10", displayTime: "08/10 16:05", branch: "台北商旅", operator: "林怡君", type: "Check-in", target: "訂單", targetIdentifier: "BK...0184", description: "訂單BK...0184 已辦理入住" },
                { date: "2026-08-11", displayTime: "08/11 11:38", branch: "台北商旅", operator: "張雅婷", type: "Check-out", target: "訂單", targetIdentifier: "BK...024", description: "訂單BK...024 已辦理退房" },
                { date: "2026-08-11", displayTime: "08/11 12:10", branch: "高雄商旅", operator: "系統管理員", type: "停用帳號", target: "員工帳號", targetIdentifier: "kaohsiung03", description: "員工帳號kaohsiung03 已停用" }
            ];

            // 2. 鎖定日期上限為今天（2026-08-11）
            const today = new Date();
            const year = today.getFullYear();
            const month = String(today.getMonth() + 1).padStart(2, '0');
            const day = String(today.getDate()).padStart(2, '0');
            const todayStr = `${year}-${month}-${day}`;

            // 3. 交叉防呆限制邏輯
            function updateDateConstraints() {
                dateStart.setAttribute("max", todayStr);
                if (dateStart.value) {
                    dateEnd.setAttribute("min", dateStart.value);
                }
                if (dateEnd.value) {
                    if (dateEnd.value < todayStr) {
                        dateStart.setAttribute("max", dateEnd.value);
                    } else {
                        dateStart.setAttribute("max", todayStr);
                    }
                } else {
                    dateEnd.setAttribute("max", todayStr);
                }
            }

            updateDateConstraints();

            dateStart.addEventListener("change", function() {
                if (dateEnd.value && dateStart.value > dateEnd.value) {
                    dateStart.value = dateEnd.value;
                }
                if (dateStart.value > todayStr) {
                    dateStart.value = todayStr;
                }
                updateDateConstraints();
            });

            dateEnd.addEventListener("change", function() {
                if (dateStart.value && dateEnd.value < dateStart.value) {
                    dateEnd.value = dateStart.value;
                }
                if (dateEnd.value > todayStr) {
                    dateEnd.value = todayStr;
                }
                updateDateConstraints();
            });

            // 4. 搜尋與過濾繪製邏輯
            btnSearch.addEventListener("click", function() {
                const sDate = dateStart.value;
                const eDate = dateEnd.value;
                const branch = branchSelect.value;
                const type = typeSelect.value;
                const operator = operatorInput.value.trim().toLowerCase();
                const target = targetInput.value.trim().toLowerCase();

                if (sDate > eDate) {
                    alert("開始日期不能大於結束日期！");
                    return;
                }

                const filtered = mockData.filter(item => {
                    // 日期篩選
                    if (item.date < sDate || item.date > eDate) return false;

                    // 【修正】防止全部分館比對失敗
                    if (branch !== "全部分館" && branch !== "" && item.branch !== branch) return false;

                    // 操作類型篩選
                    if (type !== "全部類型" && item.type !== type) return false;

                    // 操作者與受影響對象模糊搜尋
                    if (operator && !item.operator.toLowerCase().includes(operator)) return false;
                    if (target && !item.target.toLowerCase().includes(target) && !item.targetIdentifier.toLowerCase().includes(target)) return false;

                    return true;
                });

                resultTableBody.innerHTML = "";

                if (filtered.length > 0) {
                    filtered.forEach(item => {
                        const tr = document.createElement("tr");
                        // 【修正】補齊為符合 HTML 標題要求的 7 個 <td> 儲存格，徹底解決歪斜
                        tr.innerHTML = `
                                    <td class="text-secondary">${item.displayTime}</td>
                                    <td>${item.branch}</td>
                                    <td>${item.operator}</td>
                                    <td><span class="badge bg-light text-dark border">${item.type}</span></td>
                                    <td>${item.target}</td>
                                    <td class="font-monospace fw-semibold">${item.targetIdentifier}</td>
                                    <td class="small text-muted">${item.description}</td>
                                `;
                        resultTableBody.appendChild(tr);
                    });
                    resultCard.classList.remove("d-none");
                    noDataCard.classList.add("d-none");
                } else {
                    resultCard.classList.add("d-none");
                    noDataCard.classList.remove("d-none");
                }
            });
        });
    </script>
}
