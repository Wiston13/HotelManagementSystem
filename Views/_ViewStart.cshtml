@{
    var controller = ViewContext.RouteData.Values["controller"]?.ToString();

    Layout = controller is "EmployeeHome" or "Stay" or "RoomStatus"
        ? "_BranchLayout"
        : "_CustomerLayout";
}
