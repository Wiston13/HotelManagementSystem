@{
    var controller = ViewContext.RouteData.Values["controller"]?.ToString();

    Layout = controller is "EmployeeHome" or "Stay" or "RoomStatus" or "BranchBooking"
        ? "_BranchLayout"
        : "_CustomerLayout";
}
